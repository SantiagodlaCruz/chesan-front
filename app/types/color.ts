export interface Color {
  id: number;
  name: string;
  hex_code: string | null;
}

export interface Size {
  id: number;
  name: string;
}

export interface UnitMeasure {
  id: number;
  name: string;
}
